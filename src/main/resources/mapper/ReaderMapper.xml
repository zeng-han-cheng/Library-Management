<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.library.mapper.ReaderMapper">
<select id="findByUsername" resultType="Reader">SELECT * FROM reader WHERE username=#{username}</select><select id="findById" resultType="Reader">SELECT * FROM reader WHERE id=#{id}</select><select id="page" resultType="Reader">SELECT * FROM reader WHERE (#{keyword} IS NULL OR reader_no LIKE CONCAT('%',#{keyword},'%') OR username LIKE CONCAT('%',#{keyword},'%') OR real_name LIKE CONCAT('%',#{keyword},'%')) ORDER BY id DESC LIMIT #{offset},#{limit}</select><select id="count" resultType="long">SELECT COUNT(*) FROM reader WHERE (#{keyword} IS NULL OR reader_no LIKE CONCAT('%',#{keyword},'%') OR username LIKE CONCAT('%',#{keyword},'%') OR real_name LIKE CONCAT('%',#{keyword},'%'))</select><select id="countBorrowing" resultType="int">SELECT COUNT(*) FROM borrow_record WHERE reader_id=#{readerId} AND status=1</select><insert id="insert" parameterType="Reader" useGeneratedKeys="true" keyProperty="id">INSERT INTO reader(reader_no,username,password,real_name,phone,email,max_borrow,status) VALUES(#{readerNo},#{username},#{password},#{realName},#{phone},#{email},COALESCE(#{maxBorrow},5),COALESCE(#{status},1))</insert><update id="update" parameterType="Reader">UPDATE reader SET reader_no=#{readerNo},username=#{username},real_name=#{realName},phone=#{phone},email=#{email},max_borrow=#{maxBorrow},status=#{status}<if test="password != null and password != ''">,password=#{password}</if> WHERE id=#{id}</update><update id="updatePassword">UPDATE reader SET password=#{password} WHERE id=#{id}</update><delete id="delete">DELETE FROM reader WHERE id=#{id}</delete></mapper>
