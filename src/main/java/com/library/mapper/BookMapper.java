package com.library.mapper; import com.library.model.entity.Book; import com.library.model.vo.BookVO; import org.apache.ibatis.annotations.Param; import java.util.List; import java.util.Map;
public interface BookMapper { Book findById(Long id); List<BookVO> page(@Param("keyword") String keyword,@Param("categoryId") Long categoryId,@Param("status") Integer status,@Param("offset") int offset,@Param("limit") int limit); long count(@Param("keyword") String keyword,@Param("categoryId") Long categoryId,@Param("status") Integer status); int insert(Book item); int update(Book item); int delete(Long id); int decreaseStock(Long id); int increaseStock(Long id); List<Map<String,Object>> categoryStats(); Map<String, Object> overviewStats(); Map<String, Object> availableOverview(); int repairInventory(); }
